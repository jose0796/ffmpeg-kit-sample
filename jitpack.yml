install:
  - ls -R
  - FILE="-Dfile=ffmpeg-kit-full-6.0-2.aar"
  - mvn install:install-file $FILE -DgroupId=com.ffmpeg.sample -DartifactId=ffmpeg -Dversion=1.0.16 -Dpackaging=aar -DgeneratePom=true
  # Run Maven build to resolve all dependencies
  # - mvn clean install -DskipTests=true